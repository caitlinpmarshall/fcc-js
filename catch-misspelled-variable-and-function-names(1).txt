let receivables = 10;
let payables = 8;
let netWorkingCapital = receivables - payables;
console.log(netWorkingCapital)
console.log(`Net working capital is: ${netWorkingCapital}`);

//spelling mistakes can be caught with ReferenceError; it will say "thisVARiable" is not defined, so then you can see that oh, it should be "thisVariable"